name: IPTV DVR
on:
  push:
    branches:
      - main
  
jobs:
  start:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, 'ci skip')"
    
    steps:
      - name: ffmpeg
        uses: FedericoCarboni/setup-ffmpeg@v1
        id: setup-ffmpeg
        
      - name: test
        run: echo "TEST IS $TEST" && TEST1=$(cat .env) && echo "TEST1 IS $TEST1"
        
      - name: SSH
        uses: mxschmitt/action-tmate@v3.11
